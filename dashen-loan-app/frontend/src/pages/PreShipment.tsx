import Card from '../components/common/Card';

const PreShipment = () => {
  return (
    <div className="max-w-4xl mx-auto p-6">
      <h2 className="text-2xl font-bold text-primary-800 mb-6">Pre-Shipment</h2>
      <Card>
        <p className="text-gray-600">Pre-shipment financing tools will be enabled once the export workflow is configured.</p>
      </Card>
    </div>
  );
};

export default PreShipment;
